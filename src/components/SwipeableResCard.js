import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SwipeableResCard = () => {
  return (
    <View>
      <Text>SwipeableResCard</Text>
    </View>
  );
};

export default SwipeableResCard;

const styles = StyleSheet.create({});
